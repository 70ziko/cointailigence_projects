const logger = require('hgi-logger')

module.exports.notFoundMiddleware = (req, res, next) => {
  res.status(404).end()
}

module.exports.errorMiddleware = (err, req, res, next) => {
    logger.error(err.stack)
    res.status(400).json('Wystąpił błąd - skontaktuj się z administratorem').end()
}