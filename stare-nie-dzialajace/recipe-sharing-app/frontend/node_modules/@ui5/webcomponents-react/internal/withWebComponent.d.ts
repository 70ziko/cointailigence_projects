import React, { Ref } from 'react';
import { CommonProps } from '../interfaces/CommonProps';
import { Ui5DomRef } from '../interfaces/Ui5DomRef';
export interface WithWebComponentPropTypes extends CommonProps {
    ref?: Ref<any>;
    children?: any | void;
}
export declare const withWebComponent: <T extends Record<string, any>>(tagName: string, regularProperties: string[], booleanProperties: string[], slotProperties: string[], eventProperties: string[]) => React.ForwardRefRenderFunction<Ui5DomRef, T & WithWebComponentPropTypes>;
