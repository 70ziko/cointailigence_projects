import { WithWebComponentPropTypes } from '@ui5/webcomponents-react/lib/withWebComponent';
import '@ui5/webcomponents/dist/Tokenizer';
import { FC, ReactNode } from 'react';
export interface TokenizerPropTypes extends WithWebComponentPropTypes {
    showMore?: boolean;
    disabled?: boolean;
    onTokenDelete?: (event: CustomEvent) => void;
    onShowMoreItemsPress?: (event: CustomEvent) => void;
    children?: ReactNode[];
}
declare const Tokenizer: FC<TokenizerPropTypes>;
export { Tokenizer };
